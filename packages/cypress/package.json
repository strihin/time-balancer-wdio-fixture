{
  "name": "@benchmarks/cypress",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "ts-node -r tsconfig-paths/register src/runner.ts run",
    "pretest:ui": "rm -rf .logs",
    "test:ui": "ts-node -r tsconfig-paths/register src/runner.ts open"
  },
  "dependencies": {
    "@benchmarks/shared": "workspace:*"
  },
  "devDependencies": {
    "cypress": "^15.12.0",
    "mocha": "^11.7.5"
  }
}
