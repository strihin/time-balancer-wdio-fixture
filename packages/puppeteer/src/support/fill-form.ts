import { fillCheckoutForm } from '@support/checkout';

/**
 * Alias kept for backwards compatibility — delegates to fillCheckoutForm.
 * Fills the checkout step-1 form and waits for the overview page.
 */
export { fillCheckoutForm as fillForm };

